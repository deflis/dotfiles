# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name:  git-wt
description: Git worktree command completions
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.Zsh([git-wt])"]

